import styles from './lateralMenu.module.scss';

export default function LateralMenu({ children }) {
    return (
      <section className={styles.menu}>
        <h2 className="title">Planny</h2>

      </section>
    )
  }